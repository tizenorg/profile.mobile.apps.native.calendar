<manifest>
	<define>
                <domain name="${PKGNAME}" policy="shared" />
	</define>
	<assign>
		<filesystem path="/usr/share/icons/default/small/org.tizen.calendar.png" label="_"/>
	</assign>
	<request>
                <domain name="${PKGNAME}"/>
	</request>
</manifest>
